import styled from '@emotion/styled'
import LoadingButton from '@mui/lab/LoadingButton';

export const DefaultButton = styled(LoadingButton)`
  border-radius: 16px;
  box-shadow: none;
  padding: 8px 20px;
`